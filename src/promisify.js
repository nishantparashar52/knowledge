function result() {}

const exampleFn = function(x,y, callback){}

const promisify = Promisified(exampleFn);
promisify.then().then
function Promisified(fn) {
    return new Promise((resolve) => {
    })
}